# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/

## Ignore dir containing nodejs/node_modules as `walk.go` will otherwise try to walk the entire node_modules tree
## and attempt & fail to create a Secret from some files in there.
/scenarios/blackhole-access/external-services
